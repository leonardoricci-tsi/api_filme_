services:
  api:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env

  migrate:
    build: .
    env_file:
      - .env
    entrypoint: ["alembic"]
    command: ["upgrade", "head"]
    profiles:
      - tools
